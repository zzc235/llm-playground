### 前后两次提交本地与云端服务器上的内容不匹配不连续导致服务端拒收
### 处理方法:修改本地端文件名然后重新上传
### git拒绝合并
### 处理方法：点击我同意，批准非同源项目合并
### 工作日志总结
    已完成：公开仓库创建（因为之前都是自己学，不知道不公开不展示贡献）
            重新温习一下git，本质代码提交和保存的记录，但是云端和本地交接导致的不同交接协议今天触发三种，分别是提交目的，fast-forward（即快进：也就是后一次提交的开头要接上上一次提交的结尾）以及合并，第二次的代码与远端库的代码不是同一个来源（所谓祖先 common ancestor）导致git会报错，需要选择非同源合并才会进行库合并
            还有terminal 终端，也就是常用的powershell和cmd，不过Windows里还是powershell为主
            九月十四日下午五点五七分 
            